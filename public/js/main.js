// Main JavaScript file for CB Dummy

// Add any global JavaScript functionality here

console.log('CB Dummy - Callback Manager initialized');

// Auto-refresh functionality (optional)
// Uncomment to enable auto-refresh every 30 seconds
/*
if (window.location.pathname === '/callbacks' || window.location.pathname === '/') {
  setInterval(() => {
    location.reload();
  }, 30000);
}
*/
